{
  "name": "ja-system---teste",
  "version": "1.12.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "node setup.js",
    "prepare": "git config core.hooksPath githooks",
    "migrate": "node db/migrate.js",
    "migrate:new": "node db/new-migration.js",
    "teste:up": "docker compose -p jasystem-test -f compose.yaml -f compose.test.yaml --env-file .env.test up -d --build",
    "teste:down": "docker compose -p jasystem-test -f compose.yaml -f compose.test.yaml --env-file .env.test down",
    "teste:reset": "docker compose -p jasystem-test -f compose.yaml -f compose.test.yaml --env-file .env.test down -v && docker compose -p jasystem-test -f compose.yaml -f compose.test.yaml --env-file .env.test up -d --build",
    "teste:migrate": "docker compose -p jasystem-test -f compose.yaml -f compose.test.yaml --env-file .env.test exec app npm run migrate",
    "teste:logs": "docker compose -p jasystem-test -f compose.yaml -f compose.test.yaml --env-file .env.test logs -f app",
    "prod:up": "docker compose -p jasystem-prod -f compose.yaml up -d --build",
    "prod:migrate": "docker compose -p jasystem-prod -f compose.yaml exec app npm run migrate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "Bug-fix": "npm version patch && git push --follow-tags",
    "Novidade": "npm version minor && git push --follow-tags",
    "Breaking": "npm version major && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@xmldom/xmldom": "^0.8.13",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "expres": "^0.0.5",
    "express": "^4.21.2",
    "express-rate-limit": "^8.6.2",
    "flatpickr": "^4.6.13",
    "helmet": "^8.3.0",
    "imapflow": "^1.7.7",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "node-forge": "^1.4.0",
    "nodemailer": "^7.0.3",
    "nodemon": "^3.1.0",
    "pg": "^8.14.1",
    "scrollreveal": "^4.0.9",
    "xlsx": "^0.18.5",
    "xml-crypto": "^6.1.2"
  },
  "description": "",
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "acorn": "^8.17.0",
    "eslint": "^9.23.0",
    "eslint-plugin-react": "^7.37.4",
    "globals": "^16.0.0",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "libxml2-wasm": "^0.7.1",
    "supertest": "^7.2.2"
  }
}
